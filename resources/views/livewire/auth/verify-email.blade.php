<x-layouts::auth :title="__('Email verification')"><div class="grid gap-6 text-center"><x-auth-header :title="__('Verify your email')" :description="__('Click the verification link we sent to your email address.')" />@if(session('status')==='verification-link-sent')<p class="rounded-xl bg-emerald-50 p-3 text-sm font-semibold text-emerald-700">A new verification link has been sent.</p>@endif<form method="POST" action="{{ route('verification.send') }}">@csrf<x-ui.button type="submit" class="w-full">Resend verification email</x-ui.button></form><form method="POST" action="{{ route('logout') }}">@csrf<x-ui.button variant="ghost" type="submit" data-test="logout-button">Log out</x-ui.button></form></div></x-layouts::auth>
