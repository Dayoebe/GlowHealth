<x-layouts::auth :title="__('Two-factor authentication')"><div x-data="{ recovery: @js($errors->has('recovery_code')) }" class="grid gap-6"><x-auth-header :title="__('Two-factor authentication')" :description="__('Enter your authenticator code or an emergency recovery code.')" /><form method="POST" action="{{ route('two-factor.login.store') }}" class="grid gap-5">@csrf<div x-show="!recovery"><x-ui.input name="code" label="Authentication code" inputmode="numeric" autocomplete="one-time-code" maxlength="6"/></div><div x-show="recovery" x-cloak><x-ui.input name="recovery_code" label="Recovery code" autocomplete="one-time-code"/></div><x-ui.button type="submit" class="w-full">Continue</x-ui.button><button type="button" class="text-sm font-bold text-orange-700" @click="recovery=!recovery" x-text="recovery ? 'Use an authentication code' : 'Use a recovery code'"></button></form></div></x-layouts::auth>
